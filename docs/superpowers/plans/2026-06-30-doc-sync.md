# Documentation Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sync existing docs so future agents can read repo docs first and continue without chat context.

**Architecture:** Docs-only update. `AGENTS.md` remains source of truth; `README.md`, `DESIGN.md`, `DEPLOYMENT.md`, and `CLAUDE.md` are corrected to match it.

**Tech Stack:** Markdown docs, Next.js 16 App Router, Firebase RTDB/Auth, Vercel Functions, ESP32 firmware v6.5.13.

## Global Constraints

- Do not change app behavior.
- Do not add dependencies.
- Do not reintroduce attendance, price fields, Firebase Cloud Functions, or anomaly display scope.
- User-facing docs may use Indonesian where existing docs do.
- Source of truth: `AGENTS.md`.

---

## File Structure

- Modify: `README.md` — user/dev overview and agent handoff pointer.
- Modify: `DESIGN.md` — architecture/current system design.
- Modify: `DEPLOYMENT.md` — deploy/runbook details.
- Modify: `CLAUDE.md` — agent mirror facts.

### Task 1: Sync Stale Docs

**Files:**
- Modify: `README.md`
- Modify: `DESIGN.md`
- Modify: `DEPLOYMENT.md`
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: current facts from `AGENTS.md`.
- Produces: docs without known stale values: firmware `6.5.13`, heartbeat `~5s`, offline `>9s`, eval `1s`, scan mode command polling `2.5s`, no Cloud Functions, no price schema, no attendance, no anomaly display scope.

- [ ] **Step 1: Read target docs**

Run: open/read `README.md`, `DESIGN.md`, `DEPLOYMENT.md`, `CLAUDE.md`.
Expected: stale values identified before editing.

- [ ] **Step 2: Patch docs minimally**

Update only stale sections. Keep prose style intact.

- [ ] **Step 3: Verify stale strings**

Run: `rg "6\.4\.1|6\.2|8s|15s|functions/index|Anomaly Detection|unitPrice|totalAmount|price" README.md DESIGN.md DEPLOYMENT.md CLAUDE.md`
Expected: no invalid remaining stale claims; allowed matches only where explicitly saying price fields must not exist or migration strips legacy fields.

- [ ] **Step 4: Review diff**

Run: `git diff -- README.md DESIGN.md DEPLOYMENT.md CLAUDE.md docs/2026-06-30-doc-sync-design.md docs/superpowers/plans/2026-06-30-doc-sync.md`
Expected: docs-only diff.
